<nav class="bg-purple border-gray-200 h-14 px-2 sm:px-4 rounded dark:bg-gray-900 text-white">
  <div class="container flex flex-wrap justify-between items-center mx-auto">
    <div class="hidden w-full md:block md:w-auto" id="navbar-default">
      <ul class="flex flex-col p-4 mt-4 rounded-lg border border-gray-100 md:flex-row md:space-x-8 md:mt-0 md:text-sm md:font-medium md:border-0 ">
        <li>
          <a href="{{ path('app_content_home') }}" class="block py-2 pr-4 pl-3 md:p-0 hover:text-gray-300" aria-current="page">Accueil</a>
        </li>
        <li>
          <a href="{{ path('app_content_volunteer') }}" class="block py-2 pr-4 pl-3 md:p-0 hover:text-gray-300">Guide du bénévole</a>
        </li>
        <li>
          <a href="{{ path('app_calendar') }}" class="block py-2 pr-4 pl-3 md:p-0 hover:text-gray-300">Planning des bénévoles</a>
        </li>
        {% if app.user %}
          {% if is_granted('ROLE_MANAGER') %}
          <li>
            <a href="{{ path('app_user_index') }}" class="block py-2 pr-4 pl-3 md:p-0 hover:text-gray-300">Annuaire</a>
          </li>
        {% endif %}
        <li>
          <a href="{{ path('app_user_edit') }}" class="block py-2 pr-4 pl-3 md:p-0 hover:text-gray-300">Mes informations</a>
        </li>
        <li>
          <a href="{{ path('app_logout') }}" class="block py-2 pr-4 pl-3 md:p-0 hover:text-gray-300">Déconnexion</a>
        </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>
