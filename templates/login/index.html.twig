{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
  <h1>Connexion</h1>
  <div class="md:flex flex-row space-x-10">
    <div class="basis-1/3 md:basis-2/3 lg:basis-1/2 xl:basis-1/3">
      <div class="mb-6">
        <p>Pour accéder au planning des bénévoles, entrez simplement votre adresse e-mail.</p>
        <p>Si votre adresse n'est pas reconnue ou que vous souhaitez devenir bénévole, merci de nous écrire en utilisant
          le
          <b><a href="{{ path('app_contact') }}" class="link">formulaire de contact</a></b>.</p>
      </div>
      {% if error %}
        <div class="p-4 mb-4 text-sm text-red-700 bg-red-100 rounded-lg dark:bg-red-200 dark:text-red-800"
             role="alert">
          {{ error.messageKey|trans(error.messageData, 'security') }}
        </div>
      {% endif %}

      <form action="{{ path('app_login') }}" method="post">
        <div class="flex items-center border-2 px-3 rounded-2xl bg-white mb-4">
          <input type="email" placeholder="Adresse e-mail" id="username" name="_username" value="{{ last_username }}"
                 class="pl-2 py-2 w-full outline-none border-none"/>
        </div>


        <input class="hidden" type="password" id="password" name="_password" value="bypass"/>
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
        <button type="submit"
                class="block w-full bg-purple hover:bg-purple-600 mt-4 py-2 rounded-2xl text-white font-semibold mb-2 transition duration-300 ease">
          Connexion
        </button>
      </form>
    </div>
  </div>
{% endblock %}
